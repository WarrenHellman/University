<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="/style.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/skeleton/2.0.4/skeleton.min.css">
  <title>Gummy Bear Family</title>
</head>
<body>
  <div class="container">
    <h1>We are the gummy bears</h1>
    <% for(let i=0; i<members.length; i++) { %>
      <% if (members[i].age<10) {%>
        <img src="/young.jpg">
      <%}%>
      <% if(members[i].age>=10 && members[i].age<25) { %>
        <img src="/mid.jpg">
      <%}%>
      <% if(members[i].age>=25 && members[i].age<65) { %>
        <img src="/dad.jpg">
      <%}%>
      <% if (members[i].age>=65) { %>
        <img src="/old.png">
      <%}%>
      <p>Name: <a href="/bears/<%= members[i]._id %>"><%= members[i].name %></a></p>
      <p>Age: <%= members[i].age %></p>
      <p>About: <%= members[i].about %></p>
      <form id="editBTN" action="/bears/edit/<%= members[i]._id %>" method="GET">
        <button type="submit">Edit</button>
      </form>
      <form id="deleteBTN" action="/bears/delete/<%= members[i]._id %>" method="POST">
        <button type="submit" onclick="alert('are you sure?')">Delete</button>
      </form>
      
      <hr>
    <% } %>
    <!-- if age range, show photo -->
    <form action="/bears/new" method="get">
      <button type="submit">Add new Gummy Bear</button>
    </form>
  </div>
  
</body>
</html>